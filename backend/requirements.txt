# ---- Web server ----
fastapi
uvicorn
python-multipart          # required by FastAPI for /upload (multipart form data)

# ---- Config ----
python-dotenv

# ---- Audit log (database.py) ----
sqlalchemy                # async ORM used by database.py  (was missing -> import crash)
aiosqlite                 # async SQLite driver for  sqlite+aiosqlite://  (was missing)

# ---- RAG core: LlamaIndex + NVIDIA NIM + Pinecone ----
llama-index
llama-index-vector-stores-pinecone
llama-index-llms-nvidia
llama-index-embeddings-nvidia
llama-index-postprocessor-nvidia-rerank   # OPTIONAL NVIDIA NIM reranker (public SaaS EOL'd 2026-05-18; used only when RERANK_TRY_NVIDIA=true, e.g. self-hosted NIM)

# ---- Ingestion helpers ----
beautifulsoup4
requests
pypdf

# ---- Reranking: local cross-encoder (the primary, real reranker) ----
# NVIDIA's hosted reranker reached end-of-life (HTTP 410 Gone) on 2026-05-18, so
# reranking now runs LOCALLY by default: sentence-transformers powers a
# cross-encoder/ms-marco-MiniLM-L-6-v2 rerank ($0/query, offline, never retired).
# Pulls in torch (a few hundred MB). If omitted, the pipeline still degrades to a
# dependency-free BM25 lexical reranker — correct, just less semantically precise.
# Pinned <4: sentence-transformers 4/5/6 hard-require torch>=2.5, which has NO
# x86_64-macOS wheel (capped at 2.2.2). The 3.x line runs on BOTH the Linux deploy
# (torch 2.5+) and Intel-mac dev. On Intel mac also pin `numpy<2` (torch 2.2.2 was
# built against NumPy 1.x); Linux with torch 2.5+ is fine with NumPy 2.
sentence-transformers>=3.0,<4
pypdf>=4.0
